architecture: standalone
usePassword: true

auth:
  existingSecret: redis-auth
  existingSecretPasswordKey: password

master:
  persistence:
    enabled: true
    size: 8Gi
  service:
    type: LoadBalancer

metrics:
  enabled: true

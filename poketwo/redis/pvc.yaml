apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: redis-data-redis-master-0
spec:
  storageClassName: rook-ceph-block-retain
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 64Gi
